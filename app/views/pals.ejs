<%- include('partials/head') %>
<body>
    <div class="container">
        <%- include('partials/navbar') %>

        <% for(var i=0; i < list.length; i++) { %>
                <% if (list[i]._id == user){%>
                    <h1><% var array = list[i].follows%></h1>
                <% } %>
        <% } %>


            <% if (list.length > array.length){%>
            <% for(var i=0; i < list.length; i++) { %>
                <% if (list[i]._id == user){%>
                    <h1><% var array = list[i].follows%></h1>
                <% } else{%>
                        <%if (array.indexOf(list[i]._id) == -1){ %>
                    <div class="" style="border: 2px solid red;width:100%;height:20%;">
                    <p> <%= list[i].username%> </p>
                    <h4>Name: <%= list[i].name %></h4>
                    <h4>Email: <%= list[i].email %></h4>
                    <form method="post" action="/api/follow/">
                      <input type="text" hidden name="uid" value="<%= list[i]._id%>"/>
                      
                        <button type="submit" onClick=>Follow</button>
                    </form>
                    </div>
                    <hr/>
                    <% } %>
                <% } %>
            <% } %>
            <% } else{ %>  
            <h4>You are all alone here :(</h4>
            <%}%>
</div>
</div>
</body>

</html>